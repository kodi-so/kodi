{
  "name": "@kodi/db",
  "version": "0.0.0",
  "private": true,
  "exports": {
    ".": "./src/index.ts",
    "./schema": "./src/schema/index.ts",
    "./meeting-bot-identity": "./src/lib/meeting-bot-identity.ts"
  },
  "scripts": {
    "db:generate": "drizzle-kit generate",
    "db:migrate": "bun run src/migrate.ts",
    "db:migrate:kit": "drizzle-kit migrate",
    "db:push": "drizzle-kit push",
    "db:studio": "drizzle-kit studio",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "drizzle-orm": "^0.45.1",
    "postgres": "^3.4.4"
  },
  "devDependencies": {
    "@kodi/typescript-config": "*",
    "drizzle-kit": "^0.31.0",
    "typescript": "^5.4.5"
  }
}
