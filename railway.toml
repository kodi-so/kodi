[build]
  builder = "DOCKERFILE"

[deploy]
  preDeployCommand = "cd packages/db && bun run db:migrate"

# Each service is configured individually in the Railway dashboard.
# See README.railway.md for setup instructions.
# Pre-deploy commands are in per-service railway.toml files (e.g. apps/api/railway.toml).
